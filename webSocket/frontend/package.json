{
  "name": "live-chat-frontend",
  "scripts": {
    "start": "http-server",
    "test": "vitest --run --environment happy-dom --reporter verbose"
  },
  "devDependencies": {
    "happy-dom": "^17.4.6",
    "vitest": "^3.1.2"
  },
  "dependencies": {
    "http-server": "^14.1.1",
    "jsonwebtoken": "^9.0.2"
  }
}
